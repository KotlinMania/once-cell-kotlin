# Port targets — `once-cell-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `once_cell` that codex-rs uses, and therefore the minimum subset that `once-cell-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-85] (rank `12`, tier `direct`, status `To Do`)

**Cargo package name**: `once_cell`
**Rust module name (in `use` statements)**: `once_cell`

## Summary

- **1** distinct symbols imported by codex-rs
- **4** total `use` statements
- **4** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `once_cell::sync::Lazy`

- **4 `use` site(s)** across **4 codex member crate(s)**: `codex-core`, `codex-git-utils`, `codex-login`, `codex-shell-command`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/test_support.rs:23` (`codex-core`) — `use once_cell::sync::Lazy`
- `git-utils/src/apply.rs:1` (`codex-git-utils`) — `use once_cell::sync::Lazy`
- `login/src/auth/storage.rs:29` (`codex-login`) — `use once_cell::sync::Lazy`
- `shell-command/src/command_safety/windows_dangerous_commands.rs:2` (`codex-shell-command`) — `use once_cell::sync::Lazy`
