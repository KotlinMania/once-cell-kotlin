# Rust callers — `once-cell-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `once_cell` | `once_cell` | 41 | 18 | 14 | 4 |

## `once_cell`

### Inbound symbols (4 distinct, 41 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `once_cell`.

#### `once_cell::sync::Lazy` — 28 site(s) (4 codex, 24 other, 11 caller crates)

- ✅ `codex-kotlin/codex-core` `src/test_support.rs:23` — `use once_cell::sync::Lazy`
- ✅ `codex-kotlin/codex-git-utils` `src/apply.rs:1` — `use once_cell::sync::Lazy`
- ✅ `codex-kotlin/codex-login` `src/auth/storage.rs:29` — `use once_cell::sync::Lazy`
- ✅ `codex-kotlin/codex-shell-command` `src/command_safety/windows_dangerous_commands.rs:2` — `use once_cell::sync::Lazy`
- `arc-swap-kotlin/arc-swap` `benches/background.rs:7` — `use once_cell::sync::Lazy`
- `arc-swap-kotlin/arc-swap` `tests/random.rs:7` — `use once_cell::sync::Lazy`
- `arc-swap-kotlin/arc-swap` `tests/stress.rs:14` — `use once_cell::sync::Lazy`
- `insta-kotlin/insta` `src/env.rs:12` — `use once_cell::sync::Lazy`
- `insta-kotlin/insta` `src/glob.rs:6` — `use once_cell::sync::Lazy`
- `insta-kotlin/insta` `src/runtime.rs:25` — `use once_cell::sync::Lazy`
- `insta-kotlin/insta` `src/settings.rs:1` — `use once_cell::sync::Lazy`
- `insta-kotlin/insta` `src/snapshot.rs:6` — `use once_cell::sync::Lazy`
- _… and 16 more site(s)._

#### `once_cell::sync::OnceCell` — 10 site(s) (0 codex, 10 other, 8 caller crates)

- `dotenvy-kotlin/dotenvy` `tests/integration/util/testenv.rs:1` — `use once_cell::sync::OnceCell`
- `libwebrtc-kotlin/livekit-uniffi` `src/log_forward.rs:16` — `use once_cell::sync::OnceCell`
- `reqwest-kotlin/reqwest` `src/dns/hickory.rs:6` — `use once_cell::sync::OnceCell`
- `runfiles-kotlin/cargo-bazel` `src/utils/starlark/label.rs:6` — `use once_cell::sync::OnceCell`
- `serial-test-kotlin/serial_test` `src/code_lock.rs:2` — `use once_cell::sync::OnceCell`
- `starlark-kotlin/starlark` `src/environment/globals.rs:9` — `use once_cell::sync::OnceCell`
- `starlark-kotlin/starlark` `src/environment/methods.rs:4` — `use once_cell::sync::OnceCell`
- `syntect-kotlin/syntect` `src/parsing/regex.rs:1` — `use once_cell::sync::OnceCell`
- `syntect-kotlin/syntect` `src/parsing/syntax_set.rs:19` — `use once_cell::sync::OnceCell`
- `tempfile-kotlin/tempfile` `src/env.rs:6` — `use once_cell::sync::OnceCell as OnceLock`

#### `once_cell::unsync::OnceCell` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `starlark-kotlin/starlark` `src/values/types/enumeration/enum_type.rs:13` — `use once_cell::unsync::OnceCell`
- `starlark-kotlin/starlark` `src/values/types/record/record_type.rs:13` — `use once_cell::unsync::OnceCell`

#### `once_cell::race::OnceBox` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `rustls-kotlin/rustls` `src/crypto/mod.rs:706` — `use once_cell::race::OnceBox`
